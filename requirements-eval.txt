# Reproducing the evaluation and the figures.
# Install with: python3 -m pip install -r requirements-eval.txt
#
# Adds the analysis stack on top of the runtime requirements.

-r requirements.txt

# Statistics and figures
numpy==2.2.6
scipy==1.15.3
matplotlib==3.10.8

# Process-model fitness and precision in eval/privacy_utility.py, which skips
# those metrics with a warning when pm4py is absent. The correctness baseline
# does not need it: eval/baseline.py reconstructs variants with the standard
# library alone.
pm4py==2.7.22
