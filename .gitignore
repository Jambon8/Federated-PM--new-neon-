# Virtual environment
fed_env/

# Python cache
__pycache__/
*.pyc
*.pyo

# Runtime — regenerated on every run
Player-Data/
vendor/logs/

# MP-SPDZ installation — large binaries, reinstall via:
#   python3 vendor/setup.py install-mpspdz
vendor/temp/

# NEON debug logging config (auto-generated)
vendor/config/debug_logging.conf

# Agent config
.claude/
CLAUDE.md
.codex/
AGENTS.md
.mcp.json

# Thesis sources, advisor feedback, and third-party reference theses
Thesis/

# Event logs. Ignored by default so that a stray multi-gigabyte log never
# lands in a commit; the logs Chapter 6 actually measures are tracked
# explicitly and stay tracked. Add a new one with: git add -f <path>
data/

# Stray root-level LaTeX build artifacts from thesis compiles
/*.aux
/*.bbl
/*.blg
/*.fdb_latexmk
/*.fls
/*.log
/*.out
/*.synctex.gz
/*.toc

# Evaluation. Everything the thesis rests on is tracked: one CSV per
# experiment, the verifier outputs, the provenance manifest, the generated
# figure data, and every raw per-run record. Records are gzipped, which takes
# the complete set from 3.7 GB to 21 MB. Uncompressed records stay out, so a
# fresh run does not add a 100 MB file to a commit by accident.
eval_results/*
!eval_results/*.csv
!eval_results/*.json
!eval_results/scaling_plots/
!eval_results/stage_breakdown/
!eval_results/*/
eval_results/*/*.json

# Generated command lists
eval/commands*.txt

# Cluster orchestration. The published harness targets local execution;
# the SLURM/CLAIX wrappers stay local as a provenance record.
eval/claix_*.sh
eval/cluster_*
eval/run_on_hpc.sh
